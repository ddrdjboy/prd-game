# 短教程与自动灵敏度 UI 设计

| 项 | 内容 |
|---|---|
| 日期 | 2026-09-26 |
| 状态 | 实现基线 |

## 短教程

- 首次进入对局（`phase === playing`）且 `localStorage.fi45_tutorial_seen !== '1'` 时，覆盖一层说明  
- 文案 4 条：777 拉步数与事件 → 选项 → 落点 → 约会防衰减  
- 「知道了」写入 `fi45_tutorial_seen=1` 并关闭  
- 首页增加「玩法说明」按钮可再次打开（不依赖 seen）  

不改 `GamePhase`，纯 UI。

## 灵敏度

- `SET_SENSITIVITY` 已有逻辑：`low` / `standard` / `high`  
- TopBar 增加循环按钮：少打断 → 标准 → 多打断  
- 文案短标签适配竖屏  

## 测试

- `isCriticalPending`：low 仅 bankrupt/poach；high 任意 pendingDecision  
- （教程为 localStorage UI，不做脆弱 DOM 测）  

## 文件

- `TopBar.tsx` / `PlayScreen.tsx` / `HomeScreen.tsx`  
- `tests/autoSensitivity.test.ts`  
- 本设计文档  
